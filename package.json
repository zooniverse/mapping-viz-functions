{
  "name": "mapping-viz-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "func start",
    "test": "jest",
    "init-db": "psql -d mapping_viz_local < ./db/schema.sql && psql -d mapping_viz_local < ./db/data/test_data.sql "
  },
  "dependencies": {
    "pg": "^8.3.3"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
