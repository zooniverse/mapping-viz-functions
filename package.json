{
  "name": "mapping-viz-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "func start",
    "test": "jest",
    "init-db": "psql -d $PGDATABASE < ./db/schema.sql && psql -d $PGDATABASE < ./db/data/test_data.sql "
  },
  "dependencies": {
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
